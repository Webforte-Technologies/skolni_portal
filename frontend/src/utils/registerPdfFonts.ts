export function registerCzechFonts(_doc: any) {
  // Simple fallback: rely on built-in fonts; hook for future VFS font registration
  // If custom TTFs are added to public/fonts and VFS is prepared, map them here
  // doc.addFileToVFS('Inter-Regular.ttf', INTER_REGULAR_BASE64);
  // doc.addFont('Inter-Regular.ttf', 'Inter', 'normal');
  // doc.setFont('Inter', 'normal');
}


